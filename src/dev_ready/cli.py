"""CLI entry point. Wiring only — no generation logic lives here.

Responsibilities (see docs/architecture.md, Module Boundary):
- argument parsing and command dispatch
- mapping typed exceptions to exit codes and messages
"""


def main() -> int:
    """Entry point for `uvx dev-ready`. Bootstrap stub, replaced in Phase 1."""
    print("dev-ready 0.1.0 (bootstrap skeleton — implementation pending)")
    print("See docs/cli-spec.md for the planned command interface.")
    return 0
