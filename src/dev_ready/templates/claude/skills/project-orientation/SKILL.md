---
name: project-orientation
description: Orient in this project's structure before making changes - stack, commands, and where things live. Use at the start of a session in this repo.
---

# Project orientation

This project was scaffolded by dev-ready on top of fastapi/full-stack-fastapi-template.

Before making changes:

1. Read `CLAUDE.md` at the repo root for the stack, commands, and layout.
2. Read `docs/architecture.md` and `docs/requirements.md`, if they exist, for design context.
3. Backend code lives in `backend/`, frontend code in `frontend/`.

Use `docker compose watch` to run the stack locally with live reload while iterating.
