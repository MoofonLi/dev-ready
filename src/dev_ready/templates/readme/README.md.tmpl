# {{project_name}}

{{project_name}} is a full-stack FastAPI project — a FastAPI + SQLModel + PostgreSQL backend and a React + TypeScript + Vite frontend, wired together with Docker Compose. It was scaffolded by [dev-ready](https://github.com/MoofonLi/dev-ready) on top of [fastapi/full-stack-fastapi-template](https://github.com/fastapi/full-stack-fastapi-template).

## Stack

- **Backend**: FastAPI, SQLModel, PostgreSQL, Pydantic — `backend/`
- **Frontend**: React, TypeScript, Vite — `frontend/`
- **Infra**: Docker Compose, Traefik reverse proxy — `compose.yml`

## Commands

- `docker compose up -d` — start the full stack (backend, frontend, database, proxy)
- `docker compose watch` — start with live reload for local development
- `docker compose down` — stop the stack
- Backend only: `cd backend && uv run fastapi dev app/main.py`
- Frontend only: `cd frontend && npm install && npm run dev`
- Backend tests: `cd backend && bash scripts/test.sh`

## First login

The backend creates one superuser the first time it starts.

- **Email**: `admin@example.com`
- **Password**: generated for this project — it is the `FIRST_SUPERUSER_PASSWORD` value in `.env`.

`.env` also holds this project's database password and application signing key, and it is ignored by git so those never enter your history. Keep it that way.

The superuser is looked up by email and created only when no user has that address, so **editing `FIRST_SUPERUSER_PASSWORD` after the first start changes nothing** — the row already exists and the create is skipped. To pick your own password, edit it before the first `docker compose up`, or reset the database afterwards with `docker compose down -v`.

## Documentation

- Per-service setup lives in `backend/README.md` and `frontend/README.md`.
- `AGENTS.md` — canonical guidance for AI agents working in this repo (added by dev-ready).
{{clone_guidance}}
## Credits

Generated by [dev-ready](https://github.com/MoofonLi/dev-ready). Base template: [fastapi/full-stack-fastapi-template](https://github.com/fastapi/full-stack-fastapi-template) (MIT).
